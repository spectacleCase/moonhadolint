import {
  "spectacleCase/moonhadolint",
  "moonbitlang/core/encoding/utf8",
  "moonbitlang/core/env",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  "native-stub": [ "runtime.c" ],
  targets: { "runtime.mbt": [ "native" ], "main.mbt": [ "native" ] },
)
